#pragma once
#include "types.hpp"
#define FOR _for
#define FORIN foreach
using ll = i64;
#define INT(...) dR(int, __VA_ARGS__)
#define LL(...) dR(ll, __VA_ARGS__)
#define STR(...) dR(std::string, __VA_ARGS__)
#define CHR(...) dR(char, __VA_ARGS__)
#define DBL(...) dR(ld, __VA_ARGS__)
